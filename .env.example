# OpenRouter API key — used by the AI teacher to score sentences.
# Get a free key at https://openrouter.ai/keys
# This file is for documentation only; the app reads the key from localStorage
# at runtime via the in-app setup screen.
VITE_OPENROUTER_API_KEY=sk-or-your-key-here